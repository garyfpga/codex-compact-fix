Refresh to `openai/codex` `upstream/main` at
`2b44896c5ad653a1dcfc537f8bdc37767744ed09`, preserving the compact-fix fork's
bounded remote-compaction attempts, timeout and fallback behavior, compact-only
service-tier handling, metadata-based `.mod` version display, and session-only
`/model` versus persistent `/modelp` selection behavior.
