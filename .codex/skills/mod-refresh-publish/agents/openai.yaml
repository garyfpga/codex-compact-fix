interface:
  display_name: "Mod Refresh Publish"
  short_description: "Tag and publish Codex mod release"
  default_prompt: "Use $mod-refresh-publish to publish either a mod refresh release or current-HEAD mod release."
