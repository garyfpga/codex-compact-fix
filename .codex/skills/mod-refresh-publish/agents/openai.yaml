interface:
  display_name: "Mod Refresh Publish"
  short_description: "Tag and publish Codex mod release"
  default_prompt: "Use $mod-refresh-publish to tag and publish the final mod-refresh GitHub release."
