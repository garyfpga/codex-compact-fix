interface:
  display_name: "Mod Refresh Build"
  short_description: "Build Linux Codex .mod artifact"
  default_prompt: "Use $mod-refresh-build to build the Linux Codex CLI .mod artifact after a mod-refresh merge."
